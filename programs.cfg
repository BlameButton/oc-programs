{ --This is an example for a programs.cfg file. Please do not add any comments inside actual programs.lua files
  ["restutils"] = {
    files = {
      ["master/restutils/restutils.lua"] = "/lib",
    },
    name = "Rest Utils", --This is for "oppm info"
    description = "Rest Utils for easier interacting with API's", --This is for "oppm info"
    authors = "Bram Ceulemans, Raphael Bunck", --This is for "oppm info"
    note = "",
    hidden = false, -- Add this optional line to make your package not visible in "oppm list", useful for custom dependency libraries
    repo="tree/master/restutils" --Used by the website. This is where the package will link to on the website
  },
  ["json"] = {
    files = {
      ["master/json/json.lua"] = "/lib",
    },
    name = "JSON Utils", --This is for "oppm info"
    description = "JSON Utils for easier interacting with API's", --This is for "oppm info"
    authors = "Bram Ceulemans, Raphael Bunck", --This is for "oppm info"
    note = "",
    hidden = false, -- Add this optional line to make your package not visible in "oppm list", useful for custom dependency libraries
    repo="tree/master/json" --Used by the website. This is where the package will link to on the website
  },
}
